/**
 * Utility class for generating route paths for the application.
 */
export class Links {
  /** Returns the path to the home page. */
  static HomePage = () => '/';
  /** Returns the path to the projects page. */
  static ProjectsPage = () => '/projects';
  /** Returns the path to the about page. */
  static AboutPage = () => '/about';
  /** Returns the path to the contact page. */
  static ContactPage = () => '/contact';
  /** Returns the path to the quotes page. */
  static QuotesPage = () => '/quotes';
  /** Returns the path to the random joke page. */
  static RandomJokePage = () => '/random-joke';
  /** Returns the path to the skills page. */
  static SkillsPage = () => '/skills';
  /** Returns the path to the recommends page. */
  static RecommendsPage = () => '/recommends';
}
